#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "arbre.h"

int main(int argc, char **argv){
    Arbre* test =malloc(sizeof(Arbre*));
    test = creationFeuille();
    
    //initialiser();
    return 0;
}
